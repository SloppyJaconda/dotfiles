# sample hyprlock.conf
# for more configuration options, refer https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock
#
# rendered text in all widgets supports pango markup (e.g. <b> or <i> tags)
# ref. https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock/#general-remarks
#
# shortcuts to clear password buffer: ESC, Ctrl+U, Ctrl+Backspace
#
# you can get started by copying this config to ~/.config/hypr/hyprlock.conf
#
source = $HOME/.config/hypr/mocha.conf
$font = CaskaydiaCove Nerd Font

$accent = $mauve
$accentAlpha = $mauveAlpha
# $resolve.font=
# 
# I liked this font on HyDE but not enough to keep that evironment around
# Anurati|https://font.download/dl/font/anurati.zip

decoration:shadow = true

general {
  hide_cursor = false
}

animations {
  enabled = true
    bezier = linear, 1, 1, 0, 0
    animation = fadeIn, 1, 5, linear
    animation = fadeOut, 1, 5, linear
    animation = inputFieldDots, 1, 2, linear
}

background {
  monitor =
    path = ~/.config/wallpapers/current.png 
    color = rgba(95, 65, 110, 1.0)
# blur_passes = 3
# path = screenshot
}

# INPUT FIELD
input-field {
  monitor =
    size = 500, 100
    outline_thickness = 4
    dots_size = 0.2
    dots_spacing = 0.2
    dots_center = true
    outer_color = $accent
    inner_color = $surface0
    font_color = $text
    fade_on_empty = true
    placeholder_text = <span foreground="##$textAlpha"><i>󰌾 Logged in as </i><span foreground="##$accentAlpha">$USER</span></span>
    hide_input = false
    check_color = $accent
    fail_color = $red
    fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i>
    capslock_color = $yellow
    position = 0, 50
    halign = center
    valign = bottom
    shadow_passes = 2
}

# TIME
label {
  monitor =
    text = $TIME # ref. https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock/#variable-substitution
    font_size = 90
    font_family = $font
    color = $text

    position = 0, 200
    halign = center
    valign = center
    shadow_passes = 2
}

# DAY
label {
  monitor =
    text = cmd[update:1000] echo "$(date +"%A" | sed 's/./& /g' | tr '[:lower:]' '[:upper:]')"
    color = $text
    font_size = 110
    halign = center
    valign = center
    position = 0, 350
    shadow_passes = 2
}

# DATE
label {
  monitor =
    text = cmd[update:60000] date +"%d %B %Y" # update every 60 seconds
    font_size = 25
    font_family = $font

    shadow_passes = 2
    color = $text
    position = 0, 50
    halign = center
    valign = center
}

image {
  monitor = 
    path = ~/.face
    size = 150
    border_color = $accent

    shadow_passes = 2
    position = 0, 205
    halign = center
    valign = bottom
}
$dumbtext = what did you expect from furry trash?
label {
  monitor =
    text = $dumbtext
    halign = center
    valign = bottom
    font_size = 25
    font_family = $font
    color = $accent
    position = 0, 375
    shadow_passes = 2
#    onclick = $dumbtext = "uwu" 
}

#label {
#    monitor =
#    text = $LAYOUT[en]
#    font_size = 24
#    onclick = hyprctl switchxkblayout all next
#
#    position = 250, -20
#    halign = center
#    valign = center
#}
